<!--separação da barra de navegação dependendo se o usuário está conectado ou não.-->
<% if !user_signed_in? %>
  <%= render '/layouts/nav_guest' %>
<% else %>
  <%= render '/layouts/nav_user' %>
<% end %>